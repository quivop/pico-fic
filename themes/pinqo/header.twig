<!DOCTYPE html>
<html lang="en" class="no-js">
<head>
    <meta charset="utf-8" />

    <title>{% if meta.title %}{{ meta.title }} | {% endif %}{{ site_title }}</title>
    
    {% if meta.description %}
    <meta name="description" content="{{ meta.description|striptags }}">
    {% endif %}

    {% if meta.robots %}
    <meta name="robots" content="{{ meta.robots }}">
    {% endif %}

    <meta name="viewport" content="width=device-width, initial scale=1">

    <link rel="stylesheet" href="{{ theme_url }}/normalize.css?v=1" type="text/css" />
    <link rel="stylesheet" href="{{ theme_url }}/style.css?v=1" type="text/css" />

    <script src="{{ theme_url }}/scripts/modernizr-2.6.1.min.js"></script>
</head>

<body>

    <div class="container">
        <header id="sitetitle">
            <nav>
                <a href="{{ 'index'|link }}">Home</a>
                <a href="{{ 'ficlist'|link }}">Fic List</a>
            </nav>

            <h1>
                <a href="{{ 'index'|link }}">{{ site_title }}</a>
            </h1>          
        </header>
